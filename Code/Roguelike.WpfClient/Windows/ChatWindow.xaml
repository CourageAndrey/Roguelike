<windows:RoguelikeWindow x:Class="Roguelike.WpfClient.Windows.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:windows="clr-namespace:Roguelike.WpfClient.Windows"
        xmlns:converters="clr-namespace:Roguelike.WpfClient.Converters"
        Title="ChatWindow" Height="600" Width="800" Loaded="loaded" WindowStartupLocation="CenterScreen">
	<Window.Resources>
		<converters:AvailableConverter x:Key="availableConverter" />
	</Window.Resources>
	<DockPanel LastChildFill="True">
		<TextBox IsReadOnly="True" DockPanel.Dock="Top" Name="_textName" />
		<TextBox IsReadOnly="True" DockPanel.Dock="Top" Name="_textSocialGroup" />
		<TextBox IsReadOnly="True" DockPanel.Dock="Top" Name="_textAttitude" />
		<ListBox DockPanel.Dock="Right" Name="_listTopics" PreviewMouseDown="topicMouseDown">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<Label Content="{Binding Path=Text, Mode=OneTime}" Foreground="{Binding Path=IsAvailable, Mode=OneTime, Converter={StaticResource availableConverter}}" MouseDown="topicMouseDown" />
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
		<WebBrowser Name="_dialogArea" />
	</DockPanel>
</windows:RoguelikeWindow>
