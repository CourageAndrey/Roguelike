<local:RoguelikeWindow x:Class="Roguelike.WpfClient.ChoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Roguelike.WpfClient"
        SizeToContent="WidthAndHeight" MinHeight="200" MinWidth="200">
	<Window.Resources>
		<local:ItemSelectedConverter x:Key="validationConverter" />
		<local:AvailableConverter x:Key="availableConverter" />
		<local:BorderConverter x:Key="borderConverter" />
	</Window.Resources>
	<DockPanel LastChildFill="True">
		<StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Name="buttonsPanel">
			<Button Content="{Binding Path=UiCommonOk, Mode=OneTime, Converter={StaticResource borderConverter}}" Click="okClick" IsEnabled="{Binding ElementName=listBox, Path=SelectedItem, Mode=OneWay, Converter={StaticResource validationConverter}}" />
			<Button Content="{Binding Path=UiCommonCancel, Mode=OneTime, Converter={StaticResource borderConverter}}" Click="cancelClick" />
		</StackPanel>
		<ListBox Name="listBox">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<Label Content="{Binding Path=Text, Mode=OneTime}" Foreground="{Binding Path=IsAvailable, Mode=OneTime, Converter={StaticResource availableConverter}}" MouseDoubleClick="itemSelectClick" />
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</DockPanel>
</local:RoguelikeWindow>
